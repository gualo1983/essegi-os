# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: essegi-os
description: Sistema minimale immutabile ideale per me.
base-image: ghcr.io/ublue-os/silverblue-main
image-version: 44

modules:
  - from-file: moduli-segreti/modules/rpm.yml
    source: local
  - from-file: moduli-segreti/modules/flatpak.yml
    source: local
  
  - type: script
    scripts:
      - moduli-segreti/modules/install-cie.sh


  # 5. Modulo di firma (Obbligatorio alla fine)
  - type: signing
